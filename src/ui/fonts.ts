import { Antonio, League_Spartan as Spartan } from 'next/font/google'

export const antonio = Antonio({
  subsets: ['latin'],
  variable: '--font-antonio',
})

export const spartan = Spartan({
  subsets: ['latin'],
  variable: '--font-spartan',
})
